type Query {
  notes(ids: [String]!): [Note]!
  note(id: String!): Note
}

type Note {
  title: String!
  content: String!
}

type Mutation {
  dummy: Int
}

type Subscription {
  dummy: Int
}

schema {
  query: Query
  mutation: Mutation
  subscription: Subscription
}
